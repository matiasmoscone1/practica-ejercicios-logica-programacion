




//29. Patrón de números en espiral: Escribe un programa que genere un patrón de 
//números en forma de espiral cuadrada. 
 
export default function generarPatronEspiral(btn, btnLimpiar, resultado){

    const $btnGenerar = document.querySelector(btn);
    const $btnLimpiar = document.querySelector(btnLimpiar);
    const $resultadoPatronEspiral = document.querySelector(resultado);

    $btnGenerar.addEventListner("click", () => {
        const n = 5;

        //primero debo crear una matriz con dos ciclos que recorra mientras que sea menor o
        //igual a n (n)


        //tengo que declarar las variables de valor y limiteSuperior, limiteInferior, 
        //limiteIzquierdo y limiteDerecho


        //creo un ciclo while de valor = 1 mientras que sea menor o igual que n * n

        //creo 4 ciclos for dentro del while

    });



}










